# API URL (defaults to /api proxy in development)
VITE_API_URL=

# Environment indicator (pre | pro)
# Shows a banner when running in pre-production
VITE_APP_ENV=pre

# Admin auth (placeholder for future)
VITE_ADMIN_AUTH_ENABLED=false
